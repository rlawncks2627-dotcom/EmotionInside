# 감정Inside — 작업 정리

마지막 정리: 2026-08-04 (기준 커밋 `de8d142`)

초등학교 교실에서 아침마다 학생이 오늘의 감정과 컨디션을 남기고,
담임이 그 흐름을 한눈에 보는 웹 앱입니다.

---

## 1. 한눈에 보기

| 항목 | 내용 |
| --- | --- |
| 학생 화면 | React 19 + Vite 7 + TypeScript (`src/`) |
| 교사 화면 | 아직 예전 HTML 한 장 (`public/legacy/teacher.html`) |
| 백엔드 | Supabase (`mssvyuaockgepxhjmljt`, ap-northeast-2) |
| 배포 | GitHub Pages — `https://rlawncks2627-dotcom.github.io/EmotionInside/` |
| 사용 기기 | 교실 공용 태블릿 한 대를 학생들이 줄 서서 돌려 씀 |

---

## 2. 화면 흐름

### 학생 (로그인 없음)

```
학급코드 입력 ─▶ 명단에서 내 이름 ─▶ 감정 + 컨디션 + 한마디 ─▶ 완료 ─▶ 다시 명단
   (기기에 저장)      (제출한 사람은 표시)                              (다음 학생)
```

- **학급코드** — 5글자. 학교약칭(2) + 학년(1) + 반(2). 예: `YS201`
  기기에 저장해서 다음부터는 안 묻습니다. 학생이 누구인지는 **절대 저장하지 않습니다**
  (저장하면 다음 학생이 앞사람 화면으로 들어가 버림 — `src/lib/storage.ts`).
- **감정 고르기** — 캐릭터 9종 중 하나. 데이터는 DB `emotions` 테이블에서 옵니다.
- **컨디션 점수** — 1~5점을 따로 받습니다. 감정과 컨디션은 다를 수 있어서
  (웃고 있어도 몸이 힘든 날) 분리했습니다. `src/data/messages.ts`의 `CONDITION_LEVELS`.
- **한마디** — 선택 입력, 200자 제한. 안내문구는 들어올 때마다 랜덤 (`NOTE_HINTS`).
- **하루 1회** — DB에서 막습니다. 중복이면(23505) 오류 대신 완료 화면으로 보냅니다.

### 교사

`public/legacy/teacher.html` 한 파일 안에 화면 5개가 들어 있습니다.

| 화면 | 하는 일 |
| --- | --- |
| `auth` | 아이디·비밀번호 로그인 (이메일 아님) |
| `signup` | 교사가 직접 가입 신청 → 관리자는 승인/반려만 |
| `setup` | 학급 정보, 학급코드 확인 |
| `roster` | 명단 표 편집 (번호·이름) |
| `board` | 오늘 현황 + 주간 추이 차트 |

주간 추이는 감정 valence가 아니라 **컨디션 점수(1~5)** 로 그립니다.

---

## 3. 폴더 구조

```
EmotionInside/
├─ index.html                  학생 앱 진입점 (Vite)
├─ vite.config.ts              base:'./' — Pages 하위경로 대응, 번들은 dist/bundle 로
├─ .env / .env.example         Supabase 공개 키 (일부러 커밋함, 아래 5-3 참고)
├─ src/
│  ├─ App.tsx                  화면 전환 상태기계 (loading/code/roster/emotion/done)
│  ├─ types.ts                 Emotion, RosterEntry, SavedStudent, DoneResult
│  ├─ screens/                 CodeScreen, RosterScreen, EmotionScreen, DoneScreen
│  ├─ components/Toast.tsx     알림
│  ├─ data/messages.ts         안내문구, 컨디션 5단계
│  ├─ lib/
│  │  ├─ supabase.ts           클라이언트 (VITE_ 환경변수 필수)
│  │  ├─ api.ts                fetchEmotions / fetchRoster / submitCheckin, ApiError
│  │  ├─ assets.ts             asset(), characterTile() — BASE_URL 붙여 경로 생성
│  │  └─ storage.ts            학급코드만 기억, 학생은 기억 안 함
│  └─ styles/student.css       학생 화면 전체 스타일 (510줄)
├─ public/
│  ├─ assets/                  글꼴(Pretendard subset), 캐릭터 타일, 배경, 교사용 CSS
│  └─ legacy/                  index.html(구판), teacher.html(교사 화면)
├─ assets-source/              원본 webp (배포 대상 아님)
├─ 감정Inside_교사용.html      바로가기 (배포 주소로 3초 뒤 이동)
└─ 감정Inside_학생용.html      바로가기
```

---

## 4. 데이터베이스

### 테이블 (전부 RLS 켜짐)

| 테이블 | 용도 | 눈여겨볼 컬럼 |
| --- | --- | --- |
| `emotions` | 감정 9종 | `valence`(-2~+2), `bucket`(good/soso/hard), `color`, `is_watch` |
| `classes` | 학급 | `join_code`(5글자), `teacher_id` |
| `students` | 명단 | `no`, `name`, `class_id` |
| `checkins` | 기록 | `emotion_code`, `condition_score`(1~5), `note`(200자), `checkin_date` |
| `teacher_signups` | 가입 신청 | `status`(pending/approved/rejected), `password_hash` |
| `admins` | 승인 권한 계정 | 정책 안에서만 씀 |

`bucket`은 valence와 별개입니다 — 부럽이는 valence가 낮아도 담임이 긍정으로 볼 수 있습니다.

### RPC

학생용 (익명 호출, `SECURITY DEFINER`):

- `roster_by_code(p_code)` — 명단 + 오늘 제출 여부만. 기록 내용은 절대 안 나감
- `submit_checkin(p_code, p_student, p_emotion, p_note, p_score)` — 코드 검증 후 저장

교사·관리자용: `request_teacher_account`, `approve_signup`, `reject_signup`,
`create_teacher`, `provision_auth_user`, `is_admin`, `save_roster`,
`make_join_code`, `check_password_rule`

### ⚠️ 접근 설계 원칙 (중요)

학생 화면은 로그인하지 않으므로 `students`·`checkins`·`classes` 는
**익명에게 완전히 잠근 상태**로 두고, 위 RPC 두 개만 노출합니다.

> 기능이 안 될 때 "정책을 열어서" 고치고 싶어지는데, 그러면 전교생 개인정보가
> 공개 키만으로 조회됩니다. 새 데이터가 필요하면 **테이블 정책을 열지 말고
> RPC를 새로 만들거나 반환값을 넓히세요.**

함정: Supabase는 `public` 스키마 함수에 `anon`/`authenticated` EXECUTE를 자동 부여합니다.
`revoke ... from public` 은 안 먹습니다 — **`revoke ... from anon` 처럼 롤을 직접 지정**해야 합니다.
`create or replace` 를 하면 다시 붙으니 함수를 고칠 때마다 재확인하세요.

---

## 5. 개발 · 배포

### 5-1. 로컬 실행

```bash
npm install
npm run dev        # http://localhost:5173 자동 오픈
npm run typecheck  # tsc --noEmit
npm run build      # 타입검사 후 dist/ 생성
```

### 5-2. 배포

`.github/workflows/deploy.yml` — main 브랜치에 push하면 GitHub Pages로 배포.
타입 오류가 있으면 배포되지 않습니다 (`build` 스크립트가 `tsc` 를 먼저 돌림).

### 5-3. `.env` 를 커밋한 이유

들어 있는 두 값은 브라우저에 노출돼도 되는 **공개 키**이고, Pages 빌드 때 필요합니다.
접근 제어는 전부 DB의 RLS가 합니다.
**비밀 키를 추가하게 되면 그때는 반드시 `.gitignore` 에 `.env` 를 넣으세요.**

### 5-4. Supabase 프로젝트 헷갈림 주의

같은 조직에 이름이 비슷한 프로젝트가 셋 있습니다.

- ✅ `mssvyuaockgepxhjmljt` — **EmotionInside, 이게 진짜**
- ❌ `mledsqcqitpekuhetheu` — EmotionInside2
- ❌ `vlyjpligfuwpejyddpnc` — rlawncks2627-dotcom's Project (INACTIVE)

확실한 판별법은 `.env` 의 `VITE_SUPABASE_URL` 을 보는 것. 다른 프로젝트에 마이그레이션을
걸면 조용히 빈 DB를 건드리게 됩니다.

---

## 6. 지금까지 한 작업 (오래된 순)

| 커밋 | 내용 |
| --- | --- |
| `606feb8` | 학생 화면을 React + Vite + TypeScript 로 새로 구성 |
| `e0b72f5` | 학급코드를 학년·반 조합에서 5글자 규칙으로 변경 |
| `c45e85b` | 원본 포스터(`assets-source/`)를 배포 대상에서 제외 |
| `3ac0dbe` | 교사 로그인을 이메일에서 아이디·비밀번호로 변경 |
| `2ed28cc` | 교사가 직접 가입 신청하고, 관리자는 승인만 하도록 변경 |
| `0af16a7` | 명단 표 편집, 공용 태블릿 흐름(끝나면 명단 복귀), 컨디션 점수 추가 |
| `d7f519d` | 주간 추이를 감정 valence 대신 컨디션 점수로 변경, 바로가기 파일 추가 |
| `3789e90` | 컨디션 문구 정리, 감정 분류(`bucket`)를 데이터로 정의, 첫 화면 포스터·애니메이션 |
| `de8d142` | 움직임을 구름처럼 느리게, 한글 글꼴을 Pretendard 로 통일 |

---

## 7. 아직 안 끝난 것

### 커밋 안 된 변경

- **`src/styles/student.css`** — `.floater` 에 `height: auto` 추가.
  `<img>` 의 `height` 속성이 남아 있으면 `aspect-ratio` 가 무시돼서 캐릭터가
  세로로 길어지는 문제를 고친 것. 3줄짜리지만 첫 화면 모양이 달라집니다.
- **`.github/workflows/deploy.yml`** — 배포 워크플로를 만들어뒀지만 **아직 커밋·push 안 함**.
  push해야 자동 배포가 처음 돌아갑니다.
- **스크린샷 5장** (`A1-hello.png`, `A11-emotion.png`, `D-board.png`, `E-chart.png`,
  `F-roster.png`) — Playwright로 찍은 화면 확인용. 저장소에 넣을지 정해야 합니다.
  넣지 않을 거면 `.gitignore` 에 추가하세요.
- `.playwright-mcp/` — 브라우저 자동화 작업 폴더. `.gitignore` 대상입니다.

### 남은 과제

- 교사 화면(`public/legacy/teacher.html`, 1,146줄)이 아직 예전 HTML 한 장입니다.
  학생 화면처럼 React로 옮길지 결정 필요.
- `public/legacy/index.html` (구판 학생 화면, 410줄) — 새 앱이 안정되면 지울 수 있습니다.
- 테스트가 없습니다. 현재 검증 수단은 `npm run typecheck` 와 수동 확인뿐입니다.
