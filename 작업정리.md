# 감정Inside — 작업 정리

마지막 정리: 2026-08-13 (기준 커밋 `1b16c21`)

초등학교 교실에서 아침마다 학생이 오늘의 감정과 컨디션을 남기고,
담임이 그 흐름을 한눈에 보는 웹 앱입니다.

---

## 1. 한눈에 보기

| 항목 | 내용 |
| --- | --- |
| 학생 화면 | React 19 + Vite 7 + TypeScript (`src/`) |
| 교사 화면 | 아직 예전 HTML 한 장 (`public/legacy/teacher.html`) |
| 백엔드 | Supabase (`mssvyuaockgepxhjmljt`, ap-northeast-2) |
| 배포 | GitHub Pages — `https://rlawncks2627-dotcom.github.io/EmotionInside/` |
| 사용 기기 | 교실 공용 태블릿 한 대를 학생들이 줄 서서 돌려 씀 |

---

## 2. 화면 흐름

### 학생 (로그인 없음)

```
학급코드 입력 ─▶ 명단에서 내 이름 ─▶ 감정 + 컨디션 + 한마디 ─▶ 완료 ─▶ 다시 명단
   (기기에 저장)      (제출한 사람은 표시)     └▶ 선생님께 비밀편지 ──┘   (다음 학생)
                            └─ 머리말 「비밀편지」 켜고 이름 고르기 ─▶ 편지 ─┘
```

- **학급코드** — 5글자. 학교약칭(2) + 학년(1) + 반(2). 예: `YS201`
  기기에 저장해서 다음부터는 안 묻습니다. 학생이 누구인지는 **절대 저장하지 않습니다**
  (저장하면 다음 학생이 앞사람 화면으로 들어가 버림 — `src/lib/storage.ts`).
- **감정 고르기** — 캐릭터 9종 중 하나. 데이터는 DB `emotions` 테이블에서 옵니다.
- **컨디션 점수** — 1~5점을 따로 받습니다. 감정과 컨디션은 다를 수 있어서
  (웃고 있어도 몸이 힘든 날) 분리했습니다. `src/data/messages.ts`의 `CONDITION_LEVELS`.
- **한마디** — 선택 입력, 200자 제한. 안내문구는 들어올 때마다 랜덤 (`NOTE_HINTS`).
- **하루 1회** — DB에서 막습니다. 중복이면(23505) 오류 대신 완료 화면으로 보냅니다.
- **선생님께 비밀편지** — 1,000자까지. 감정 기록과 달리 하루 1회 제한이 없습니다.
  기록은 하루 한 번이지만 하고 싶은 말이 생기는 건 하루 한 번이 아니기 때문입니다.
  입구가 셋입니다.

  | 어디 | 어떻게 |
  | --- | --- |
  | 명단 화면 머리말의 학급 이름 옆 버튼 | 누르면 노랗게 켜지고, 그 상태에서 이름을 고르면 편지 화면 |
  | 오늘 기록을 마친 학생이 **자기 이름을 누르면** | 「이미 기록했어」 화면에 편지 버튼이 있음 |
  | 감정 고르기 화면 머리말 | 같은 이름의 버튼 |

  두 번째가 중요합니다. **오늘 기록을 마친 학생은 감정 화면으로 못 들어갑니다.**
  예전에는 이름을 누르면 「이미 기록했어」 알림만 뜨고 명단에 그대로 있었는데,
  편지를 보내려는 아이에게는 그 자리에서 막힌 것으로 보입니다. 그래서 이름을 누르면
  편지로 가는 길이 있는 화면으로 갑니다. 이 화면은 **자동으로 명단에 돌아가지 않습니다** —
  편지를 쓸지 고르는 동안 화면이 넘어가면 안 되기 때문입니다.
- **익명으로 보내기** — 체크하면 DB에 보낸 사람을 **아예 저장하지 않습니다**(`student_id`가 null).
  화면에서만 가리는 게 아니라 기록이 남지 않아, 나중에도 누가 썼는지 알 수 없습니다.
  대신 `is_anonymous` 를 따로 남겨 담임이 「익명으로 보내려고 한 편지」임을 알 수 있게 합니다.

### 교사

`public/legacy/teacher.html` 한 파일 안에 화면 5개가 들어 있습니다.

| 화면 | 하는 일 |
| --- | --- |
| `auth` | 아이디·비밀번호 로그인 (이메일 아님) |
| `signup` | 교사가 직접 가입 신청 → 관리자는 승인/반려만 |
| `setup` | 학급 정보, 학급코드 확인 |
| `roster` | 명단 표 편집 (번호·이름) |
| `board` | 오늘 현황 + 주간 추이 차트, 편지함 |

편지함은 머리말의 「편지함」 버튼입니다. 버튼 옆 작은 숫자가 **안 읽은 편지 수**이고,
0이면 숨깁니다. 창을 열면 최신순으로 전부 보이고, 그린 뒤에 읽음 처리합니다
(「새 편지」 표시는 그 창을 닫을 때까지 남습니다). 익명 편지는 보낸 사람 자리에
`[익명]` 이 다른 색으로 뜹니다 — 이름이 빠진 게 아니라 학생이 그렇게 보내기로
한 것임이 한눈에 보여야 하기 때문입니다.

주간 추이는 감정 valence가 아니라 **컨디션 점수(1~5)** 로 그립니다.
가로축은 **월요일부터 일요일까지 이레**입니다. 예전에는 월~금만 그렸는데, 그러면
주말에 남긴 기록이 아무 말 없이 사라졌습니다. 주말 칸은 옅게 칠해 수업일과 구분합니다.
기록이 없는 날을 건너뛴 구간은 실선이 아니라 점선으로 이어, 안 낸 날을 그은 선으로
채우지 않습니다.

---

## 3. 폴더 구조

```
EmotionInside/
├─ index.html                  학생 앱 진입점 (Vite)
├─ vite.config.ts              base:'./' — Pages 하위경로 대응, 번들은 dist/bundle 로
├─ .env / .env.example         Supabase 공개 키 (일부러 커밋함, 아래 5-3 참고)
├─ src/
│  ├─ App.tsx                  화면 전환 상태기계 (loading/code/roster/emotion/done)
│  ├─ types.ts                 Emotion, RosterEntry, SavedStudent, DoneResult
│  ├─ screens/                 CodeScreen, RosterScreen, EmotionScreen, AlreadyScreen,
│  │                           LetterScreen, DoneScreen
│  ├─ components/Toast.tsx     알림
│  ├─ data/messages.ts         안내문구, 컨디션 5단계
│  ├─ lib/
│  │  ├─ supabase.ts           클라이언트 (VITE_ 환경변수 필수)
│  │  ├─ api.ts                fetchEmotions / fetchRoster / submitCheckin, ApiError
│  │  ├─ assets.ts             asset(), characterTile() — BASE_URL 붙여 경로 생성
│  │  └─ storage.ts            학급코드만 기억, 학생은 기억 안 함
│  └─ styles/student.css       학생 화면 전체 스타일 (510줄)
├─ public/
│  ├─ assets/                  글꼴(Pretendard subset), 캐릭터 타일, 배경, 교사용 CSS
│  └─ legacy/                  index.html(구판), teacher.html(교사 화면)
├─ sql/                        Supabase 스키마 변경 기록 (날짜별)
├─ assets-source/              원본 webp (배포 대상 아님)
├─ 감정Inside_교사용.html      바로가기 (배포 주소로 3초 뒤 이동)
└─ 감정Inside_학생용.html      바로가기
```

---

## 4. 데이터베이스

### 테이블 (전부 RLS 켜짐)

| 테이블 | 용도 | 눈여겨볼 컬럼 |
| --- | --- | --- |
| `emotions` | 감정 9종 | `valence`(-2~+2), `bucket`(good/soso/hard), `color`, `is_watch` |
| `classes` | 학급 | `join_code`(5글자), `teacher_id` |
| `students` | 명단 | `no`, `name`, `class_id` |
| `checkins` | 기록 | `emotion_code`, `condition_score`(1~5), `note`(200자), `checkin_date` |
| `teacher_signups` | 가입 신청 | `status`(pending/approved/rejected), `password_hash` |
| `admins` | 승인 권한 계정 | 정책 안에서만 씀 |
| `letters` | 선생님께 보낸 비밀편지 | `is_anonymous`, `student_id`(익명이면 null), `read_at` |

`letters` 에는 제약 두 개가 걸려 있습니다. `letters_body_len`(1~1,000자)과
`letters_anon_has_no_sender` — **익명 편지에 보낸 사람이 남아 있으면 안 된다**는 것을
DB 에서 못 박습니다. 화면 코드가 바뀌어도 이 약속은 깨지지 않습니다.

`bucket`은 valence와 별개입니다 — 부럽이는 valence가 낮아도 담임이 긍정으로 볼 수 있습니다.

### RPC

학생용 (익명 호출, `SECURITY DEFINER`):

- `roster_by_code(p_code)` — 명단 + 오늘 제출 여부만. 기록 내용은 절대 안 나감
- `submit_checkin(p_code, p_student, p_emotion, p_note, p_score)` — 코드 검증 후 저장
- `send_letter(p_code, p_student, p_body, p_anonymous)` — 코드 검증 후 편지 저장.
  익명이면 `student_id` 를 넣지 않음

교사·관리자용: `request_teacher_account`, `approve_signup`, `reject_signup`,
`create_teacher`, `provision_auth_user`, `is_admin`, `save_roster`,
`make_join_code`, `check_password_rule`, `owns_class`, `mark_letters_read`

`letters` 는 담임에게 **SELECT 정책만** 줍니다. UPDATE 정책을 열면 `read_at` 뿐 아니라
편지 내용까지 고칠 수 있게 되기 때문입니다. 읽음 표시는 `mark_letters_read(p_class)` 가
대신하고, 이 함수는 `owns_class()` 로 자기 반인지부터 확인합니다.

스키마 변경은 `sql/` 에 날짜별 파일로 남깁니다 (`sql/2026-08-13_letters.sql`).

### ⚠️ 접근 설계 원칙 (중요)

학생 화면은 로그인하지 않으므로 `students`·`checkins`·`classes` 는
**익명에게 완전히 잠근 상태**로 두고, 위 RPC 두 개만 노출합니다.

> 기능이 안 될 때 "정책을 열어서" 고치고 싶어지는데, 그러면 전교생 개인정보가
> 공개 키만으로 조회됩니다. 새 데이터가 필요하면 **테이블 정책을 열지 말고
> RPC를 새로 만들거나 반환값을 넓히세요.**

함정: Supabase는 `public` 스키마 함수에 `anon`/`authenticated` EXECUTE를 자동 부여합니다.
`revoke ... from public` 은 안 먹습니다 — **`revoke ... from anon` 처럼 롤을 직접 지정**해야 합니다.
`create or replace` 를 하면 다시 붙으니 함수를 고칠 때마다 재확인하세요.

---

## 5. 개발 · 배포

### 5-1. 로컬 실행

```bash
npm install
npm run dev        # http://localhost:5173 자동 오픈
npm run typecheck  # tsc --noEmit
npm run build      # 타입검사 후 dist/ 생성
```

### 5-2. 배포

`.github/workflows/deploy.yml` — main 브랜치에 push하면 GitHub Pages로 배포.
타입 오류가 있으면 배포되지 않습니다 (`build` 스크립트가 `tsc` 를 먼저 돌림).

### 5-3. `.env` 를 커밋한 이유

들어 있는 두 값은 브라우저에 노출돼도 되는 **공개 키**이고, Pages 빌드 때 필요합니다.
접근 제어는 전부 DB의 RLS가 합니다.
**비밀 키를 추가하게 되면 그때는 반드시 `.gitignore` 에 `.env` 를 넣으세요.**

### 5-4. Supabase 프로젝트 헷갈림 주의

같은 조직에 이름이 비슷한 프로젝트가 셋 있습니다.

- ✅ `mssvyuaockgepxhjmljt` — **EmotionInside, 이게 진짜**
- ❌ `mledsqcqitpekuhetheu` — EmotionInside2
- ❌ `vlyjpligfuwpejyddpnc` — rlawncks2627-dotcom's Project (INACTIVE)

확실한 판별법은 `.env` 의 `VITE_SUPABASE_URL` 을 보는 것. 다른 프로젝트에 마이그레이션을
걸면 조용히 빈 DB를 건드리게 됩니다.

---

## 6. 지금까지 한 작업 (오래된 순)

| 커밋 | 내용 |
| --- | --- |
| `606feb8` | 학생 화면을 React + Vite + TypeScript 로 새로 구성 |
| `e0b72f5` | 학급코드를 학년·반 조합에서 5글자 규칙으로 변경 |
| `c45e85b` | 원본 포스터(`assets-source/`)를 배포 대상에서 제외 |
| `3ac0dbe` | 교사 로그인을 이메일에서 아이디·비밀번호로 변경 |
| `2ed28cc` | 교사가 직접 가입 신청하고, 관리자는 승인만 하도록 변경 |
| `0af16a7` | 명단 표 편집, 공용 태블릿 흐름(끝나면 명단 복귀), 컨디션 점수 추가 |
| `d7f519d` | 주간 추이를 감정 valence 대신 컨디션 점수로 변경, 바로가기 파일 추가 |
| `3789e90` | 컨디션 문구 정리, 감정 분류(`bucket`)를 데이터로 정의, 첫 화면 포스터·애니메이션 |
| `de8d142` | 움직임을 구름처럼 느리게, 한글 글꼴을 Pretendard 로 통일 |
| `1c86d38` | 첫 화면 캐릭터가 세로로 늘어지던 문제 수정 |
| `7e306cf` | main 에 push하면 GitHub Pages 로 배포되는 워크플로 추가 |
| `b6ac236` | 작업 정리 문서 추가 |
| `b4080cf` | 휴대폰·태블릿 화면 대응, 주간 추이에서 주말 기록이 빠지던 문제 수정 |
| `7b3c261` | 주간 컨디션 흐름을 PDF 리포트로 내려받는 기능 추가 |
| `1b16c21` | 학생이 선생님께 비밀편지를 보내고, 담임이 편지함에서 읽는 기능 추가 |

---

## 7. 휴대폰 · 태블릿 대응

교실 태블릿뿐 아니라 선생님 휴대폰으로도 열기 때문에, 두 화면 모두 폭에 맞춰 배치를 바꿉니다.
`index.html` 과 `teacher.html` 의 viewport 에 `viewport-fit=cover` 를 넣어야
`env(safe-area-inset-*)` 이 값을 가지므로, 노치·홈바를 피하는 여백이 여기에 달려 있습니다.

### 학생 화면 (`src/styles/student.css`)

감정 고르기 화면은 **스크롤 없이 한 화면에 들어와야** 합니다.
줄 서서 쓰는 태블릿에서 「다 골랐어」 버튼이 화면 밖에 있으면 아이가 그 자리에서 멈춥니다.
그래서 가로 폭만 보지 않고 세로 여유까지 함께 보고 배치를 고릅니다.

| 화면 | 배치 |
| --- | --- |
| 넓은 화면(900px~) 또는 가로로 든 기기(700px~ & 높이 700px 이하) | 감정과 메모를 나란히 |
| 세로 태블릿(600~899px & 높이 701px~) | 위아래로 두되 격자 폭을 520px 로 묶어 메모칸까지 한 화면에 |
| 휴대폰(~480px) | 여백을 좁혀 3×3 유지, 명단은 두 칸 |
| 휴대폰 가로(높이 520px 이하) | 첫 화면 캐릭터를 줄이고 글자를 낮춰 코드 입력칸을 살림 |

`.emotion` 의 `min-width: 120px` 을 뺐습니다. 그게 있으면 좁은 휴대폰에서
3칸 합이 화면보다 넓어져 타일이 잘렸습니다. 320px 기기에서도 타일 한 변이 88px 입니다.

### 교사 화면 (`public/assets/teacher.css`)

- 좌우 여백은 `--gutter` 하나로 잡고, 노치가 있으면 `max()` 로 그만큼 밀어냅니다.
- 820px 이하 — 머리말을 붙박이(sticky)에서 풀고, 도구막대를 층으로 쌓고,
  명단·학생 상세를 **전체 화면**으로 엽니다. 좁은 화면에서 가운데 뜬 작은 창은 만지기 나쁩니다.
- 520px 이하 — 명단 표의 곁칸(번호·기록·삭제)을 좁혀 이름 칸을 지키고,
  날짜 줄을 접히게 둡니다. 접히지 않으면 320px 기기에서 도구막대가 통째로 화면 밖으로 밀립니다.
- 차트는 폭에 따라 **도면 자체를 갈아끼웁니다**(`chartSvg(days, narrow)`).
  넓은 도면을 그대로 축소하면 눈금 글씨가 6px 이 됩니다. 좁은 배치에서는 세로 눈금이
  숫자만 남고, 그 뜻은 차트 아래 설명줄(`.chart-legend`)이 대신합니다.
  `matchMedia` 로 폭이 바뀌면 다시 그립니다.

---

## 8. 주간 리포트 PDF

`public/assets/report.js` — 주간 컨디션 흐름을 A4 한 장짜리 리포트로 내려받습니다.

| 어디 | 버튼 | 결과 |
| --- | --- | --- |
| 학생 상세 창 아래 | `PDF 리포트` | 그 학생, 보고 있는 주 |
| 내보내기 메뉴 | `이번 주 (반 전체)` | 명단 순서대로 학생마다 한 장 |

담은 것 — 요약 한 줄, 컨디션 꺾은선 차트, 그 주에 남긴 한마디 전부.
한마디가 많아 한 장을 넘기면 다음 장으로 이어지고, 쪽머리에 `(이어서)` 가 붙습니다.

### 왜 캔버스에 그려서 그림으로 넣는가

PDF 안에 한글을 **글자로** 넣으려면 글꼴을 통째로 심어야 해서 파일이 수 MB 로 불어납니다.
대신 브라우저가 이미 불러온 Pretendard 로 `<canvas>` 에 그린 뒤 그 그림을 얹으면,
글꼴을 한 바이트도 심지 않고 한글이 제대로 나옵니다. PDF 껍데기는 직접 씁니다(약 80줄).

- 외부 라이브러리 0개 — 교내망에서 CDN 이 막혀도 동작합니다.
- 25명 한 반이 약 1.5MB. A4 150dpi 로 그려 인쇄해도 글자가 깨지지 않습니다.
- **맞바꾼 것:** PDF 안의 글자는 그림이라 **복사·검색이 되지 않습니다.**
  나중에 텍스트를 긁어 쓸 일이 생기면 이 방식을 바꿔야 합니다.

메모리 주의: 한 쪽이 1240×1754 라서 25명치 캔버스를 한꺼번에 들면 200MB 가 넘습니다.
그래서 **한 명씩 그리고 바로 JPEG 로 굳힌 뒤 캔버스를 놓아줍니다**. 발에 전체 장수를 찍지
않는 것도 같은 이유입니다 — 전체를 알려면 모든 쪽을 다 들고 있어야 합니다.

### 화면과 리포트가 같은 차트를 그리게 하기

`chartGeometry()`, `buildWeek()`, `scoredPoints()` 는 `report.js` 가 내보내고
`teacher.html` 의 화면용 SVG 도 같은 것을 씁니다. 좌표 계산을 양쪽에 따로 두면
어느 한쪽만 고쳤을 때 화면과 종이가 다른 그림을 보여주게 됩니다.

### 기록 없는 학생도 뺀 페이지를 만들지 않습니다

반 전체 리포트는 그 주에 기록이 없는 학생도 한 장을 만듭니다. 명단 순서 그대로여야
파일철에 넣기 좋고, 조용히 빠지면 담임이 누가 안 냈는지 알아채기 어렵기 때문입니다.

---

## 9. 아직 안 끝난 것

### 남은 과제

- 교사 화면(`public/legacy/teacher.html`, 1,146줄)이 아직 예전 HTML 한 장입니다.
  학생 화면처럼 React로 옮길지 결정 필요.
- `public/legacy/index.html` (구판 학생 화면) — 새 앱이 안정되면 지울 수 있습니다.
  휴대폰·태블릿 대응은 새 React 앱에만 넣었습니다. 비밀편지는 두 화면 모두에 넣었지만,
  같은 기능을 두 벌 손봐야 하는 상태라 구판을 지우는 편이 낫습니다.
- 테스트가 없습니다. 현재 검증 수단은 `npm run typecheck` 와 수동 확인뿐입니다.
  교사 화면은 로그인이 필요해서, 검증할 때는 `teacher.html` 을 그대로 두고
  Supabase 클라이언트만 가짜로 바꾼 사본을 만들어 실제 차트 코드를 돌려봤습니다.
